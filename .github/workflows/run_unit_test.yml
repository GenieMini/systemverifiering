name: UnitTest

on:
  push:
    branches:
      - '**'

jobs:
  unittest:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Python
        uses: actions/setup-python@v3
        with:
          python-version: 3.8
      - name: Run Version
        run: |
          python --version
